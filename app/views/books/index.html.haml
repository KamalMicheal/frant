- if is_admin?
  = link_to I18n.t(:add_new), {controller: :books, action: :new}, {class: 'btn btn-sm btn-primary'}

- @books.each do |book|
  = render 'book', :book => book

%nav{class: 'blog-pagination'}
  = will_paginate @books, {class: 'blog-pagination'}